# Configurazione di base
NODE_ENV=development    # development o production

# Configurazione del database - Solo una delle seguenti sezioni è necessaria
# -------------------------------------------------------------------------

# Opzione 1: SQLite (predefinito, non richiede altre configurazioni)
DB_TYPE=sqlite          # Usa SQLite come database predefinito

# Opzione 2: PostgreSQL (opzionale, solo se si desidera usare PostgreSQL)
# DB_TYPE=postgres
# DATABASE_URL=postgres://username:password@localhost:5432/db_name

# Opzione 3: MySQL (opzionale, solo se si desidera usare MySQL)
# DB_TYPE=mysql
# MYSQL_DATABASE_URL=mysql://username:password@localhost:3306/db_name

# Opzione 4: Microsoft SQL Server (opzionale, solo se si desidera usare SQL Server)
# DB_TYPE=mssql
# MSSQL_DATABASE_URL=mssql://username:password@localhost:1433/db_name

# Segreto per le sessioni (generare un valore casuale per la produzione)
SESSION_SECRET=misericordia-secret-key-change-me-in-production

# Configurazione del server
PORT=5000               # Porta su cui verrà avviato il server
HOST=0.0.0.0           # Hostname (0.0.0.0 per accessi esterni)

# Configurazione di sicurezza
COOKIE_SECURE=false     # true in produzione se si usa HTTPS

# Impostazioni di internazionalizzazione
DEFAULT_LANGUAGE=it     # Lingua predefinita (it o en)
FORCE_ITALIAN=true      # Forza l'utilizzo dell'italiano